{
    "name": "trypescript-rtti-transformed-tests",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "tspc --build",
        "clean": "tspc --build --clean",
        "test": "npm run build && jest --runInBand"
    },
    "dependencies": {
        "typescript-rtti": "^0.9.6"
    },
    "devDependencies": {
        "chai": "^4.3.0",
        "pythagora": "^0.0.80",
        "ts-jest": "^29.0.3",
        "ts-patch": "^3.0.2",
        "typescript": "5.1.x"
    }
}
